package capabilities

// Capabilities is a lightweight stand-in for the Wails capability set.
// The gui repo only needs the type to exist so application.App can expose it.
type Capabilities struct{}
